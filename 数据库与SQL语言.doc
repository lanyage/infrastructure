1.存储过程和函数区别是什么？
  存储过程是用户自定义的一系列SQL语句的集合，涉及特定的表或其他对象的任务，用户可以调用存储过程。而函数则是数据库已经定义的方法，它接受参数并且返回
某种类型的值，而且不涉及特定用户表。
2.什么是数据库事物？
  数据库事物是指作为单个逻辑工作单元执行的一系列操作，这些操作要么全做，要么全不做，是一个不可分割的工作单元。
3.讲下聚簇索引和非聚簇索引区别？
  前者是数据的物理存储顺序，后者与数据物理排列顺序无关。SQlserver中索引是通过二叉树的数据结构来描述的，我们可以这么理解，索引的叶节点就是数据节点，而
非聚簇索引的叶节点仍然是索引节点，只不过有一个指针指向对应的数据块。
4.使用索引就一定检索的快吗？
5.游标的作用是什么？如何知道游标是否已经到了最后？
游标用来定位结果集的行，通过判断@@FETCH_STATUS可以判断游标是否到了最后，通常这个变量不等于0表示出错或者到了最后。
6.语句触发器和行触发器有什么区别？
  语句触发器可以运行在语句执行的前后，而行触发器在影响的每一行出发一次。
7.查询的数据很多时，比如说有1000万条，用什么办法可以提升查询效率？在数据库方面或者java代码方面有什么优化的办法？
  一，在数据库设计方面
  首先，建立索引
  分区，比如按时间分区
  尽量使用固定长度的字段
  限制字段长度
  二，在数据库I/O方面
  首先，增加缓冲区
  如果涉及表的忌廉，不同的表存储在不同的磁盘上，以增加I/O速度
  三，在SQL语句方面
  优化sql语句，减少比较次数。
  限制返回的条目数，也就是分页。
  四，在java方面
  如果是反复使用的查询，使用PreparedStatement减少查询次数
8. A B C
   3 7 9 
   4 8 4
   5 6 9
  选出A B字段值，并按B的升序排列
  插入一条记录
  选出C值，并按照C的降序排列，消除重复值
  
